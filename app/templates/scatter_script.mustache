var data = [
{
      strokeColor: '#F16220',
      // pointColor: '#F16220',
      pointColor: 'rgba(200,0,0,0.3)',
      // pointStrokeColor: 'rgba(255,255,255,0.3)',
      pointStrokeColor: 'rgba(100,0,0,0.5)',
      data: [
        {{{total_counts_for_scatter}}}
      ]
    }
];
var ctx = $("#myScatter").get(0).getContext("2d");
var myScatterChart = new Chart(ctx).Scatter(data, {
  datasetStroke: false,
  tooltipTemplate: "<%if (datasetLabel){%><%=datasetLabel%>: <%}%><%=argLabel%>; <%=valueLabel%>",
  multiTooltipTemplate: "<%=argLabel%>; <%=valueLabel%>"
  });
